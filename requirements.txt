arxiv>=2.1.0
tenacity>=8.0
pydantic>=2.0
sentence-transformers>=2.2,<6.0
requests>=2.28
numpy>=1.24
networkx>=3.0
socksio>=1.0
# Skill 3 — paper_summarizer is host-LLM driven; no extra API client needed.
# (Legacy standalone path uses anthropic; install from skills/paper_summarizer/requirements.txt only if you need it.)
# Skill 5 — papers-analysis-visualizer (dashboard + Notion)
jinja2>=3.0
python-dotenv>=1.0
# Optional: Skill 3 PDF mode (--mode pdf)
# pypdf>=4.0
# pdfminer.six>=20231228
